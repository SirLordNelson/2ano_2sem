import java.util.Scanner;
import java.util.Arrays;
import java.time.LocalDate;
import java.time.format.DateTimeFormatter;
import java.util.ArrayList;

public class Main2{
    public static void main(String[] args){
        Scanner scanner = new Scanner(System.in);
        /*
        Ex2 ex2 = new Ex2();

        System.out.println("Inserir tamanho do array: ");
        int length = scanner.nextInt();

        for (int i = 0; i < length; i++) {
            LocalDate date = LocalDate.now().plusDays(i);
            ex2.insereData(date);
        }

        ex2.mostraDatas();

        System.out.print("Inserir uma data (formato dd/MM/yyyy): ");
        scanner.nextLine();
        String dataStr = scanner.nextLine();

        DateTimeFormatter formatter = DateTimeFormatter.ofPattern("dd/MM/yyyy");
        LocalDate dataInserida = LocalDate.parse(dataStr, formatter);

        LocalDate dataMaisProxima = ex2.dataMaisProxima(dataInserida);

        System.out.println("Data mais próxima: " + dataMaisProxima);
        
       Ex4 ex4 = new Ex4();

       System.out.println("Inserir tamanho do array: ");
       int length = scanner.nextInt();

        ex4.strings = new String[length];

        for(int i = 0; i < length; i++){
            System.out.println("Inserir string " + (i + 1) + ": ");
            String input = scanner.next();
            ex4.strings[i] = ex4.insereString(input);
        }

        ex4.mostraString();

        String maiorString = ex4.determinaMaiorString();
        System.out.println("A maior string inserida é: " + maiorString);

        String maisRep = ex4.maisRepetida();
        System.out.println("A string inserida mais repetida é: " + maisRep);

        System.out.println("Inserir string a procurar: ");
        String str = scanner.next();
        int qntRep = ex4.strRepetida(str);
        System.out.println("A string inserida mais repetida repete-se: " + qntRep + " vezes.");
        */
       
    }
}